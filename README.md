1.Ans = > VAR = var = পুরোনো, function‑scoped, re‑declare করা যায়; let = আধুনিক, block‑scoped, re‑assign করা যায়; const = block‑scoped, কিন্তু re‑assign করা যায় না





2.Ans = > Spread operator (...) হলো কোনো array বা object‑কে ভেঙে আলাদা আলাদা মান হিসেবে ছড়িয়ে দেয়, যাতে সহজে copy, merge বা function এ argument পাঠানো যায়


3.Ans => map() নতুন array বানায় পরিবর্তিত মান দিয়ে, 
        filter() নতুন array বানায় শর্ত মেনে বাছাই করা মান দিয়ে, 
        আর forEach() শুধু loop চালায় কিন্তু নতুন array ফেরত দেয় না।





4.Ans => Arrow function হলো JavaScript‑এর একটি shorthand syntax যেখানে function keyword না লিখে ()=>{} দিয়ে ফাংশন লেখা যায়, আর এটি সাধারণত concise ও readable কোডের জন্য ব্যবহার হয়
- Arrow function → shortcut, clean, বাইরের this ধরে |







5.Ans => Template literals মানে হলো backtick দিয়ে লেখা string, যেখানে সহজে variable আর multi‑line text বসানো যায়

